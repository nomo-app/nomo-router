name: nomo_router
description: Navigator using the Navigator 2.0 API
version: 0.0.1
repository: https://github.com/nomo-app/nomo-router
website: https://nomo.app
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  animations: ^2.0.8
  route_gen: ^0.0.2
  collection: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build: any
  source_gen: any
  analyzer: any

flutter:
