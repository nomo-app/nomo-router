name: nomo_router
description: Navigator using the Navigator 2.0 API
version: 0.0.1

publish_to: none

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  animations: ^2.0.8
  route_gen: 
    path: ./route_gen
  collection: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build: any
  source_gen: any
  analyzer: any

flutter:
